# NBA Dashboard (Parquet Cached)

This app uses nba_api for NBA data and caches results to local parquet files under `data_cache/`.

## Local run

```bash
pip install -r requirements.txt
streamlit run app.py
